@model IEnumerable<DormWebApp.Areas.Admin.Models.Room>

@{
    ViewBag.Title = ViewBag.FloorNo;
}

<h2>@ViewBag.FloorNo</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table rooms-wrapper">
   <!-- <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoomNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Capacity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsAvaible)
        </th>
        <th></th>
    </tr> -->

@foreach (var item in Model) {
    <tr class="room">
        <td>
           Room number: @Html.DisplayFor(modelItem => item.RoomNo)
        </td>
        <td>
           Capacity:  @Html.DisplayFor(modelItem => item.Capacity)
        </td>
        <td class="showRoomAvaible">
           Is Avaible: <span class="avaible @(@item.Capacity == 0 ? "no" : "yes")"></span> 
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
